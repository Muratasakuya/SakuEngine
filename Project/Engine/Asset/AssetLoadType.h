#pragma once

//============================================================================
//	AssetLoadType enum class
//	アセットの読み込み方式を表す列挙。処理の同期/非同期を切り替えるために使用
//============================================================================

// 読み込みの仕方
enum class AssetLoadType {

	Synch, // 同期
	Async  // 非同期
};
